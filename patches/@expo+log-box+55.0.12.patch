diff --git a/node_modules/@expo/log-box/android/build.gradle b/node_modules/@expo/log-box/android/build.gradle
index 70c8b20..ad7ff8a 100644
--- a/node_modules/@expo/log-box/android/build.gradle
+++ b/node_modules/@expo/log-box/android/build.gradle
@@ -68,11 +68,9 @@ afterEvaluate {
       outputs.dir distDirFile
 
       // Generate the bundle
-      if (Os.isFamily(Os.FAMILY_WINDOWS)) {
-        commandLine("cmd", "/c", *nodeExecutableAndArgs, "$expoLogBoxDir/scripts/build-bundle.mjs")
-      } else {
-        commandLine(*nodeExecutableAndArgs, "$expoLogBoxDir/scripts/build-bundle.mjs")
-      }
+      // Windows build fix (persisted via patch-package): exec node directly instead
+      // of via `cmd /c`, which mis-parses the node command and exits 1.
+      commandLine(*nodeExecutableAndArgs, "$expoLogBoxDir/scripts/build-bundle.mjs")
     }
 
     // Generate bundle at preBuild
